#include <stdio.h>

int main()
{
    int x;
    long y;
    long long z;
    printf("size of int x:%d\n", sizeof(x));
    printf("size of int y:%d\n", sizeof(y));
    printf("size of int z:%d\n", sizeof(z));

    return 0;
}
